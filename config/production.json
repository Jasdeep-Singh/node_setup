{
  "app": {
    "name": "Tiger test project",
    "host": "http://x90s.com",
    "port": 2083,
    "jwtAlgo": "HS512",
    "jwtKey": "donation-project"
  },
  "db": {
    "name": "survey",
    "host": "localhost",
    "auth": false,
    "username": "",
    "password": "",
    "port": 27017,
    "mongoose": {
      "useMongoClient": true,
      "autoIndex": true, // Don't build indexes
      "poolSize": 10, // Maintain up to 10 socket connections
      // If not connected, return errors immediately rather than waiting for reconnect
      "bufferMaxEntries": 0
    }
  }
}
